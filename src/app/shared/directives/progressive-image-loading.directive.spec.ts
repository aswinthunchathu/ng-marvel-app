import { ProgressiveImageLoadingDirective } from './progressive-image-loading.directive'

describe('ProgressiveImageLoadingDirective', () => {
    it('should create an instance', () => {
        const directive = new ProgressiveImageLoadingDirective()
        expect(directive).toBeTruthy()
    })
})
